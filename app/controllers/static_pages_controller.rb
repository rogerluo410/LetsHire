# NOTE: Currently we do not show home/help/contact link on page header,
# but someday we might need them, so we keep the code here.
class StaticPagesController < ApplicationController
  def home
  end

  def help
  end

  def contact
  end
end
